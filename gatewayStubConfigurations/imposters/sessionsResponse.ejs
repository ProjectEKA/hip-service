(request, state, logger) => {
    logger.info("/v1/sessions is called");
    logger.info(request.body);
    return {
      headers: {
        'Content-Type': 'application/json'
      },
      // it is required by the framework to return some data here, but doesn't matter what it is for the purpose of this stub
      body: JSON.stringify({ accessToken: "RandomToken", tokenType: "" })
    };
  }