{{ if .Values.secrets -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "hip.fullname" . }}-secrets
  labels:
    {{- include "hip.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}